## Stack notes — Game

- Engine: {{engine}}. Editor-side changes (scenes, prefabs, assets) need explicit approval — merge conflicts there are expensive.
- Never commit generated/import caches ({{cache_dirs}}).
- Performance budget: {{perf_budget}}; profile before/after on perf-sensitive changes.
