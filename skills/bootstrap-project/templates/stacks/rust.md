## Stack notes — Rust

- `cargo clippy --all-targets` must be clean; treat warnings as errors.
- `cargo fmt` before commit. Prefer `?` propagation over `.unwrap()` outside tests.
- Feature flags in use: {{feature_flags}}.
