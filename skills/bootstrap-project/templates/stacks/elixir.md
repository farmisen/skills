## Stack notes — Elixir / Phoenix

- Run `mix precommit` (or `{{check_alias}}`) when done and fix everything it reports.
- Use `Req` for HTTP; avoid httpoison/tesla/httpc.
- Phoenix {{phoenix_version}} conventions apply; LiveView templates start with `<Layouts.app ...>`.
