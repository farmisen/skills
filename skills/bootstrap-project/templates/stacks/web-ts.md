## Stack notes — TypeScript / web

- Package manager: `{{pkg_manager}}`. Never mix lockfiles.
- Strict typing: explicit return types on exported functions; `import type` for types.
- Monorepo commands run from the root; per-package via `{{per_package_cmd}}`.
